# Task 1 Report: Rename entities + DbContext (+ ProductVariant columns)

**Status:** DONE_WITH_CONCERNS

**Commit:** `9ba9074 refactor: rename Category/CategoryProduct/Notification entities`

## What I implemented

Renamed the 3 entity CLR types in `Flower.Data` (file renames done via `git mv`, contents preserved verbatim per brief — only the class name changed):

- `Entities/Category.cs` → `Entities/PostCategory.cs` (`class PostCategory`)
- `Entities/CategoryProduct.cs` → `Entities/ProductCategory.cs` (`class ProductCategory`)
- `Entities/Notification.cs` → `Entities/CustomerNotification.cs` (`class CustomerNotification`)

Navigation/FK property renames:

- `Entities/Post.cs:30-33` — `CategoryId`/`Category` → `PostCategoryId`/`PostCategory` (with matching `[ForeignKey("PostCategoryId")]`).
- `Entities/Product.cs:35-38` — `CategoryProductId`/`CategoryProduct` → `ProductCategoryId`/`ProductCategory` (with matching `[ForeignKey("ProductCategoryId")]`).
- `Entities/ProductVariant.cs:17-20` — `PriceAdjustment` → `Price` (keeps `[Column(TypeName = "decimal(18,2)")]`), added `[MaxLength(50)] string? Sku` directly after it. `[ForeignKey("ProductId")]` nav untouched.

DbContext DbSets:

- `ApplicationDbContext.cs:14,17,34` — `Categories` → `PostCategories`, `CategoriesProducts` → `ProductCategories`, `Notifications` → `CustomerNotifications`.
- `IApplicationDbContext.cs:13,16,33` — same three renames.

OnModelCreating fixes (NOT explicitly in the brief's Step 7/8, but required for `Flower.Data` to compile — the nav/FK member names changed):

- `ApplicationDbContext.cs` Post/Product relationship config: `.HasOne(p => p.PostCategory)/.HasForeignKey(p => p.PostCategoryId)` and `.HasOne(p => p.ProductCategory)/.HasForeignKey(p => p.ProductCategoryId)`.
- `modelBuilder.Entity<Notification>()` (3 blocks) → `modelBuilder.Entity<CustomerNotification>()`. Index database names (`IX_Notifications_CustomerId`, `IX_Notifications_CustomerId_IsRead`) left unchanged (see concerns).

Did NOT touch: historical `Migrations/*` files (including `ApplicationDbContextModelSnapshot.cs`), other projects, and did not create any migration (that is Task 5).

## Verification

### `dotnet build Flower.Data`
```
Build succeeded.
0 Error(s)
20 Warning(s)
```
All 20 warnings are pre-existing `CS8618` nullable warnings on unrelated entities (User, Customer, DeliverySlot, PhoneBlacklist, PaymentMethod, ProductImage, Product, Post, and — after rename — PostCategory/ProductCategory `Name`/`Posts`). Zero errors.

### Stale-identifier grep (`Flower.Data`, excluding historical `Migrations/`)
Pattern: `\bCategory\b | \bCategoryProduct\b | \bNotification\b | CategoriesProducts | PriceAdjustment | \bCategoryId\b | \bCategoryProductId\b`

**Result: zero hits** outside `Migrations/`. The only substring hits for the looser `CategoryId`/`CategoryProductId` pattern are the new `PostCategoryId`/`ProductCategoryId` identifiers (correct). All genuine stale references now live only in historical migration `.cs`/`.Designer.cs`/`ApplicationDbContextModelSnapshot.cs` files, which are string-based snapshots that must not be hand-edited and get regenerated by Task 5's migration.

## Files changed

1. `Flower.Data/Entities/PostCategory.cs` (renamed from Category.cs)
2. `Flower.Data/Entities/ProductCategory.cs` (renamed from CategoryProduct.cs)
3. `Flower.Data/Entities/CustomerNotification.cs` (renamed from Notification.cs)
4. `Flower.Data/Entities/Post.cs`
5. `Flower.Data/Entities/Product.cs`
6. `Flower.Data/Entities/ProductVariant.cs`
7. `Flower.Data/ApplicationDbContext.cs`
8. `Flower.Data/IApplicationDbContext.cs`

## Self-review findings / concerns

1. **Index DB names kept old (moderate concern).** `OnModelCreating` still declares `HasDatabaseName("IX_Notifications_CustomerId")` and `"IX_Notifications_CustomerId_IsRead"` for `CustomerNotification`. Task 5's hardcoded migration renames those DB indexes to `IX_CustomerNotifications_*`. After Task 5 the model will expect the old names, so a later `dotnet ef migrations add` may want to rename them back. The Task 1 brief did not ask to change these, so I left them; Task 5 (Step 4, designer review) or a follow-up should reconcile them.
2. **Historical migrations keep old names — expected.** `20260722230554_AddPagesAndContacts.cs`, `.Designer.cs`, `20260729004843_AddProductImages.*`, `20260730105026_AddCategoryImageAndAvatar.*`, and `ApplicationDbContextModelSnapshot.cs` still reference `Category`/`CategoryProduct`/`Notification`/`PriceAdjustment`/`CategoriesProducts`. This is correct EF behavior (applied migrations are immutable). Snapshot regeneration happens in Task 5.
3. **Scope respected.** No files outside `Flower.Data` were touched (verified via `git status` — the `.superpowers/sdd/*` and plan-file changes in the worktree predate this task and were left unstaged). No migration created.
4. **Grep definition note.** The task's grep list is interpreted as "no stale identifiers in active model code"; `Migrations/` hits are the intentional exception documented above.

## Concern fix (index DB names)

Follow-up commit `dc61bb1 refactor: rename notification index names in DbContext` addressed concern #1.

**Change:** `Flower.Data/ApplicationDbContext.cs` — the two explicit index DB names for `CustomerNotification` now follow the rename:
- `.HasDatabaseName("IX_Notifications_CustomerId")` → `.HasDatabaseName("IX_CustomerNotifications_CustomerId")`
- `.HasDatabaseName("IX_Notifications_CustomerId_IsRead")` → `.HasDatabaseName("IX_CustomerNotifications_CustomerId_IsRead")`

Nothing else changed; scope stayed within `Flower.Data`. No migration created.

**Verification:**
- `dotnet build Flower.Data` → `Build succeeded.` / `0 Error(s)`.
- Grep `Flower.Data` (excluding `Migrations/`) for `IX_Notifications_CustomerId` → **0 hits**.

