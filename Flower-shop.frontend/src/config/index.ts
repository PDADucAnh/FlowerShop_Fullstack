const CONFIG = {
  API_BASE_URL: import.meta.env.VITE_API_URL || 'https://localhost:7224',
  IMAGE_BASE_URL: import.meta.env.VITE_API_URL || 'https://localhost:7224',
  TIMEOUT: 10000,
};

export default CONFIG;
